name: build_server

on:
  push:
    branches: [ master ]
#    paths: [ server/** ]
  workflow_dispatch:

defaults:
  run:
    working-directory: server

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
            pip3 install -r requirements.txt
            pip3 install tensorflow_datasets tf_slim cx_Freeze
      - name: Copy assets
        run: |
            Invoke-WebRequest -Uri https://home.in.tum.de/~zhangja/nornsvae/assets.zip -Outfile assets.zip
            Expand-Archive assets.zip .
      - name: Build
        run: python3 setup.py build
      - name: Package
        run: Compress-Archive build/* nornsvae_server_windows.zip
      - uses: actions/upload-artifact@v3
        with:
          name: nornsvae_server_windows
          path: server/nornsvae_server_windows.zip


  build-mac:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
            pip3 install -r requirements.txt
            pip3 install tensorflow_datasets tf_slim cx_Freeze
      - name: Copy assets
        run: |
            curl https://home.in.tum.de/~zhangja/nornsvae/assets.zip -o assets.zip
            unzip assets.zip
      - name: Build
        run: python3 setup.py build
      - name: Package
        run: cd build && zip -r ../nornsvae_server_mac.zip .
      - uses: actions/upload-artifact@v3
        with:
          name: nornsvae_server_mac
          path: server/nornsvae_server_mac.zip


  release:
    runs-on: ubuntu-latest
    needs: [build-windows, build-mac]
    steps:
      - uses: actions/download-artifact@v3
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          name: Pre-release
          prerelease: true
          tag_name: release
          body: Test release (auto-generated)
          files: |
            server/nornsvae_server_windows.zip
            server/nornsvae_server_mac.zip
